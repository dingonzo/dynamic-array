    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Run configure
      # Removed the working-directory line so it stays in the root
      run: |
        chmod +x ./configure
        ./configure

    - name: make
      run: make

    - name: make check
      run: make check
